<!DOCTYPE html> 
<html>

  <head>
    <title>QuikByte Dev</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <!-- <%= javascript_include_tag :defaults %> -->
  </head>

  <body>
  <div class="logo">
<%= image_tag("logos/QuikByte Logo.png", width: '250px') %>
</div>

  <div class="buttons">
  <button class="button"><%= link_to "Home", root_path %></button>

  <% if !user_signed_in? %>
  	<button class="sign_in"><%= link_to "Login", new_user_session_path %></button>
  <% end %>

  <% if user_signed_in? %>
  	<button class="button"><%= link_to "New Post", new_post_path %></button>
  	<button class="button"><%= link_to "Logout", destroy_user_session_path, :method => :delete %></button>
  	</div>
    <p>Hello, <%= current_user.name %>! You're our favorite customer! </p>
  	<% end %>

  	<p class="notice"><%= notice %></p>
  	<p class="alert"><%= alert %></p>

    <%= yield %>
  </body>
</html>
