<%= form_for(recipes_has_ingredient) do |f| %>
  <% if recipes_has_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(recipes_has_ingredient.errors.count, "error") %> prohibited this recipes_has_ingredient from being saved:</h2>

      <ul>
      <% recipes_has_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %>
    <%= f.text_field :recipe_id %>
  </div>

  <div class="field">
    <%= f.label :ingredient_id %>
    <%= f.text_field :ingredient_id %>
  </div>
  
  <div class="field">
    <%= f.label :quantity %>
    <%= f.text_field :quantity %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
