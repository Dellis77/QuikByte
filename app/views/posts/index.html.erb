

<h1>Welcome!</h1>
<% if user_signed_in? %>

<% end %>

<% @posts.each do |post| %>
	<h3><u><%= post.title %></u></h3>
	<div><%= truncate(post.body, :lenght => 25) %></div>
	<button class="button"><%= link_to "Read Post", post %></button>
	<% end %>
